{% extends 'base.html.twig' %}

{% block title %}Welcome{% endblock %}

{% block body %}
<div class="row justify-content-center mt-5">
    <div class="col-md-8 text-center">
        <h1 class="display-4">Welcome to My Symfony App</h1>
        <p class="lead mt-3">A modern login and registration system with Bootstrap 5.</p>
        <a href="{{ path('login') }}" class="btn btn-primary btn-lg me-2">Login</a>
        <a href="{{ path('register') }}" class="btn btn-outline-primary btn-lg">Register</a>
    </div>
</div>
{% endblock %}
